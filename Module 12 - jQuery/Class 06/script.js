// $("button") = to select all tags with "button" type
// $("#btn1") = to select the tag with id="btn1"
// $(".btn") = to select all tag with class="btn"
// $(".list1 .btn") // to select a tag class inside another tag class
// selecting a element is equal to select in css 

// all commands above retorns an array, so, if I want to know if exists we use:
// if($('.btn').length > 0) {}

// I can attribute the tag element returns to a variable, like this:
// var test = $('#btn1');